fastapi>=0.115
uvicorn[standard]>=0.32
sqlalchemy[asyncio]>=2.0.36
asyncpg>=0.30
alembic>=1.18.5
pydantic>=2.9
pydantic-settings>=2.6
email-validator>=2.2
bcrypt>=4.2
PyJWT>=2.10
# Fernet, for the third-party helpdesk credentials an organization entrusts to us.
cryptography>=43.0
# Allowlist HTML sanitizer (Rust ammonia) for org-authored email bodies. Not hand-rolled:
# the rich-text editor means we accept markup, and an escape-everything policy is only safe
# while nobody can author markup at all.
nh3>=0.2.18
anthropic>=0.40
stripe>=11.0
httpx>=0.27
